specVersion: "v1beta"
serviceId: "google-map"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "google-map-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
